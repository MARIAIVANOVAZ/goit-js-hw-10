{{#each this}}

  <li class='card-item'>
    <img
      class='country-image'
      alt='{{name.official}}'
      width='40px'
      height='24px'
      src='{{flags.svg}}'
    />
    <h2 class='country-title'>{{name.official}}</h2>
  </li>
{{/each}}